{
  "name": "project-template",
  "version": "0.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/ubilabs/project-template"
  },
  "devDependencies": {
    "gh-pages": "^0.3.1"
  },
  "scripts": {
    "build": "rm -rf build && mkdir build && cp -r templates build && cp -r gh-pages/* build && cp setup.sh build",
    "deploy": "gh-pages -d build"
  }
}
